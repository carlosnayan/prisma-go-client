# 🗺️ Roadmap - Prisma for Go

This document details the complete roadmap for Prisma for Go, a type-safe ORM inspired by the original Prisma.

## 📊 Overall Status

- **Phase 1**: ✅ **COMPLETED** (Basic setup go environment)
- **Phase 2**: ✅ **COMPLETED** (Schema.prisma Parser)
- **Phase 3**: ✅ **COMPLETED** (CLI - Basic Commands)
- **Phase 4**: ✅ **COMPLETED** (Migration System)
- **Phase 5**: ✅ **COMPLETED** (Database Push and Introspection)
- **Phase 6**: ✅ **COMPLETED** (Advanced Query Builder)
- **Phase 7**: ✅ **COMPLETED** (Advanced Features)
- **Phase 8**: ✅ **COMPLETED** (Multi-Database Support)
- **Phase 9**: ✅ **COMPLETED** (Tools and Utilities)
- **Phase 10**: ✅ **COMPLETED** (Documentation and Examples)
- **Phase 11**: ✅ **COMPLETED** (Publication and Community)

---

## ✅ Phase 1: Foundation and Project Structure - **COMPLETED**

### 1.1 Directory Structure and Architecture ✅

- [x] Reorganize project structure to support CLI as a separate package
- [x] Create `cmd/prisma/` directory for main CLI
- [x] Create `internal/` directory for internal code (parser, migrations, etc.)
- [x] Modular structure created:
  ```
  /cmd/prisma/          # Executable CLI ✅
  /internal/config/     # prisma.conf management ✅
  /internal/parser/     # schema.prisma parser ✅
  /internal/migrations/ # Migration system (structure created)
  /internal/introspect/ # Database introspection (structure created)
  /internal/dialect/    # Database dialects (structure created)
  /builder/             # Query builder (already existed) ✅
  /generator/           # Code generation (already existed) ✅
  /raw/                 # Raw SQL (already existed) ✅
  ```

### 1.2 Configuration System (prisma.conf) ✅

- [x] Create parser for `prisma.conf` file (TOML format)
- [x] Support configurations equivalent to `prisma.config.ts` v7:
  - [x] `schema` - path to schema.prisma
  - [x] `[migrations]` - path and seed
  - [x] `[datasource]` - url (with environment variable support)
  - [x] `[generator]` - provider, output, previewFeatures (optional)
- [x] Implement configuration validation
- [x] Environment variable expansion support:
  - [x] `env("DATABASE_URL")` or `env('DATABASE_URL')`
  - [x] `${DATABASE_URL}` or `$DATABASE_URL`
- [x] Automatic `prisma.conf` search by traversing up directories

---

## ✅ Phase 2: Schema.prisma Parser - **COMPLETED**

### 2.1 Lexer and Tokenizer ✅

- [x] Implement lexer to tokenize schema.prisma file
- [x] Support comments (`//` and `/* */`)
- [x] Identify tokens: keywords, identifiers, types, operators, strings
- [x] Correct handling of newlines and whitespace

### 2.2 AST Parser (Abstract Syntax Tree) ✅

- [x] Create data structures to represent the schema:
  - [x] `Model` (tables)
  - [x] `Field` (columns with types, attributes, modifiers)
  - [x] `Enum` (enumerations)
  - [x] `Relation` (relationships between models - via attributes)
  - [x] `Index` (indexes and unique constraints - via attributes)
  - [x] `Datasource` (database configuration)
  - [x] `Generator` (generation configuration)

### 2.3 Type and Attribute Support ✅

- [x] Primitive types: String, Int, Boolean, DateTime, Float, Decimal, Json, Bytes
- [x] Special types: BigInt, Unsupported("")
- [x] Modifiers: `?` (optional), `[]` (array)
- [x] Field attributes: `@id`, `@unique`, `@default()`, `@relation()`, `@map()`, `@updatedAt`
- [x] Model attributes: `@@id()`, `@@unique()`, `@@index()`, `@@map()`
- [x] Functions in attributes: `env()`, `autoincrement()`, `now()`, etc.

### 2.4 Schema Validation ✅

- [x] Validate relationships (correct foreign keys)
- [x] Check compatible types
- [x] Detect duplicate names
- [x] Validate attribute syntax
- [x] Validate datasources and generators
- [x] Prisma to Go type mapping
- [x] Invalid syntax detection in attribute arguments ✅
- [x] Rejection of unsupported `map[...]` syntax ✅

---

## ✅ Phase 3: CLI - Basic Commands - **COMPLETED**

### 3.1 Native CLI ✅

- [x] Native CLI implementation in Go (`cli/` package) ✅
- [x] Removed Cobra dependency ✅
- [x] Root command: `prisma` with subcommands ✅
- [x] Global flags support: `--config`, `--schema`, `--verbose` ✅
- [x] Modular command structure ✅
- [x] Entry point: `cmd/prisma/main.go` ✅
- [x] Custom argument parser ✅
- [x] Nested subcommand support ✅

### 3.2 `prisma init` Command ✅

- [x] Create initial project structure:
  - [x] `prisma.conf` file with default configurations
  - [x] `schema.prisma` file with basic example
  - [x] `prisma/migrations/` directory
- [x] Support for `--provider` and `--database` flags
- [x] Generate example connection string (via environment variable)
- [ ] Interactive mode to choose database (PostgreSQL, MySQL, SQLite)

### 3.3 `prisma generate` Command ✅ **100% COMPLETED**

- [x] Command structure created
- [x] Read and parse `schema.prisma` ✅
- [x] Generate Go types for each model (structs with json and db tags) ✅
- [x] Generate type-safe query builders for each model ✅
- [x] Generate main `db/client.go` file with Prisma client ✅
- [x] Query builder with fluent API (chainable) ✅
- [x] Support Prisma syntax in Where() (map and direct SQL) ✅
- [x] FindFirst() and FindMany() methods as aliases ✅
- [x] Chainable methods: Where(), Order(), Limit(), Offset(), etc. ✅
- [x] Generate auxiliary types (CreateInput, UpdateInput, WhereInput) ✅
- [x] **100% type-safe Fluent API implemented** ✅
  - [x] `Create().Data(...).Exec(ctx)` with type-safe CreateInput ✅
  - [x] `FindMany().Select(...).Where(...).Exec(ctx)` with type-safe Select and WhereInput ✅
  - [x] `FindFirst().Select(...).Where(...).Exec(ctx)` with type-safe Select and WhereInput ✅
  - [x] `Update().Where(...).Data(...).Exec(ctx)` with type-safe UpdateInput ✅
  - [x] `Delete().Where(...).Exec(ctx)` with type-safe WhereInput ✅
  - [x] Select types (UserSelect, PostSelect, etc.) for field selection ✅
  - [x] WhereInput with Filter types (StringFilter, IntFilter, DateTimeFilter, etc.) ✅
  - [x] Logical operators: AND, OR, NOT in WhereInput ✅
- [ ] Relationship support (Include, Select) - Deferred to Phase 6

### 3.4 `prisma format` Command ✅

- [x] Command structure created
- [x] Format schema.prisma file ✅
- [x] Sort models, fields and attributes ✅
- [x] Apply style conventions ✅
- [x] --check flag to verify formatting ✅
- [x] --write flag to save changes ✅
- [x] Syntax validation before formatting ✅
- [x] Detection and rejection of invalid syntax (e.g., `map[...]`) ✅
- [x] Protection against infinite loop in formatting ✅
- [x] Clear error messages for invalid syntax ✅

### 3.5 `prisma validate` Command ✅

- [x] Command structure created
- [x] Validate schema.prisma syntax ✅
- [x] Check relationship consistency ✅
- [x] Report errors and warnings ✅

---

## ✅ Phase 4: Migration System - **COMPLETED**

### 4.1 `prisma migrate dev` Command ✅

- [x] Command structure created
- [x] Detect changes in schema.prisma compared to database ✅
- [x] Database introspection (PostgreSQL) ✅
- [x] Incremental comparison between schema and database ✅
- [x] Automatically generate migration SQL ✅
- [x] Create migration file in `prisma/migrations/YYYYMMDDHHMMSS_name/migration.sql` ✅
- [x] Apply migration to database ✅
- [x] Update `_prisma_migrations` table ✅
- [x] Automatically run `prisma generate` after applying ✅
- [ ] Relationship and foreign key support (deferred to Phase 6)
- [ ] Complete index and constraint support (partial)

### 4.2 `prisma migrate deploy` Command ✅

- [x] Command structure created
- [x] Apply pending migrations in production ✅
- [x] Do not generate new migrations ✅
- [x] Non-interactive mode for CI/CD ✅

### 4.3 `prisma migrate reset` Command ✅

- [x] Command structure created
- [x] Clean database (DROP SCHEMA) ✅
- [x] Recreate database ✅
- [x] Apply all migrations ✅
- [x] Run seed (if configured) ✅

### 4.4 `prisma migrate status` Command ✅

- [x] Command structure created
- [x] List applied migrations ✅
- [x] Show pending migrations ✅
- [x] Detect divergences between schema and database ✅

### 4.5 `prisma migrate resolve` Command ✅

- [x] Manually mark migration as applied ✅
- [x] Mark migration as rolled back (not applied) ✅
- [x] Resolve migration conflicts ✅

---

## ✅ Phase 5: Database Push and Introspection - **COMPLETED**

### 5.1 `prisma db push` Command ✅

- [x] Command structure created
- [x] Apply schema.prisma changes directly to database (without creating migration) ✅
- [x] Useful for rapid development ✅
- [x] Detect destructive changes and request confirmation ✅
- [x] Update database without migration history ✅
- [x] `--accept-data-loss` flag to accept destructive changes ✅
- [x] `--skip-generate` flag to skip code generation ✅

### 5.2 `prisma db pull` Command (Introspection) ✅

- [x] Command structure created
- [x] Connect to existing database ✅
- [x] Read structure (tables, columns, types, constraints, indexes) ✅
- [x] Generate schema.prisma from database ✅
- [x] Preserve correct types ✅
- [x] Convert snake_case to camelCase/PascalCase ✅
- [ ] Relationship support (deferred to Phase 6)
- [ ] Views and stored procedures support (as `Unsupported`)

### 5.3 `prisma db seed` Command ✅

- [x] Command structure created
- [x] Execute configured seed file ✅
- [x] Support seed in Go (`seed.go`) or SQL (`seed.sql`) ✅
- [x] Configuration in `prisma.conf`:
  ```toml
  [migrations]
  seed = "go run prisma/seed/seed.go"
  ```
- [x] Execution in correct project directory ✅

### 5.4 `prisma db execute` Command ✅

- [x] Command structure created
- [x] Execute arbitrary SQL on database ✅
- [x] File support (`--file`) ✅
- [x] Stdin support (`--stdin`) ✅
- [x] Direct argument support ✅
- [x] Interactive mode (read from stdin) ✅
- [x] Useful for administrative commands ✅

---

## ✅ Phase 6: Advanced Query Builder - **COMPLETED**

### 6.1 Relationships (Relations) ✅

- [x] `Include` support for eager loading ✅
- [x] `Select` support for specific fields ✅
- [x] Structure for 1:1, 1:N, N:M relationships ✅
- [x] Nested queries - basic structure ✅
- [ ] Complete relationship implementation (requires schema parsing - deferred to Phase 7)

### 6.2 Advanced Operators ✅

- [x] Basic WHERE operators (already implemented) ✅
- [x] Prisma syntax support (map) and direct SQL in Where() ✅
- [x] Operators: Gt, Gte, Lt, Lte, In, NotIn, Like, ILike, IsNull, IsNotNull ✅
- [x] Fluent API with chaining ✅
- [x] Logical operators: `AND`, `OR`, `NOT` ✅
- [x] Text operators: `Contains`, `StartsWith`, `EndsWith`, `ContainsInsensitive`, `StartsWithInsensitive`, `EndsWithInsensitive` ✅
- [x] List operators: `Has`, `HasEvery`, `HasSome`, `IsEmpty` ✅
- [ ] Relationship operators: `some`, `every`, `none`, `is`, `isNot` (deferred to Phase 7 - requires schema)

### 6.3 Aggregations ✅

- [x] `Count`, `Sum`, `Avg`, `Min`, `Max` ✅
- [x] `GroupBy` with `Having` support ✅
- [x] Generic `Aggregate` method for custom aggregations ✅
- [ ] Aggregations in relationships (deferred to Phase 7)

### 6.4 Transactions ✅

- [x] Transaction support with fluent API ✅
- [x] Interactive transactions: `ExecuteTransaction(fn)` ✅
- [x] Sequential transactions: `ExecuteSequentialTransactions([op1, op2])` ✅
- [x] Automatic rollback on error ✅
- [x] Manual `Commit` and `Rollback` methods ✅

### 6.5 Middleware and Hooks ⏳

- [ ] `beforeCreate`, `afterCreate`
- [ ] `beforeUpdate`, `afterUpdate`
- [ ] `beforeDelete`, `afterDelete`
- [ ] `beforeFind`, `afterFind`
- [ ] Global and per-model middleware
- **Note**: Deferred to Phase 7 - requires integration with hook system

---

## ✅ Phase 7: Advanced Features - **COMPLETED**

### 7.1 Validations ✅

- [x] Runtime validations via `ValidateStruct` ✅
- [x] Support for `validate` tags in structs ✅
- [x] Rules: `required`, `min=`, `max=`, `email` ✅
- [x] Validation of numeric and string types ✅
- [x] `Validator` interface for custom validations ✅
- [ ] Schema-level validations (via custom attributes - requires parser)
- [ ] Integration with Go validation libraries (validator, ozzo-validation)

### 7.2 Advanced Soft Deletes ✅

- [x] Basic support via `deleted_at` (already implemented) ✅
- [x] `Restore()` and `ForceDelete()` methods ✅
- [x] Automatic filters in queries ✅
- [x] Option to include deleted: `IncludeDeleted()` ✅
- [x] `OnlyDeleted()` method to return only deleted records ✅

### 7.3 Automatic Timestamps ✅

- [x] Basic support for `created_at` and `updated_at` (already implemented) ✅
- [x] Automatic `updated_at` update on UPDATEs ✅
- [x] Automatic `created_at` and `updated_at` insertion on INSERTs ✅
- [ ] Automatic `@createdAt` and `@updatedAt` in schema (requires parser)
- [ ] Timezone support (deferred to Phase 8)
- [ ] Format customization (deferred to Phase 8)

### 7.4 JSON Fields ✅

- [x] Operations on JSON/JSONB fields via `JSON()` ✅
- [x] `Get()` method to get JSON values ✅
- [x] `Set()` method to update JSON values ✅
- [x] `Contains()` method to check JSON keys ✅
- [x] `Path()` method to access nested paths ✅
- [ ] Type-safe JSON with generics (deferred to future version)

### 7.5 Full-Text Search ✅

- [x] Full-text search support (PostgreSQL) ✅
- [x] Search operators: `Search()`, `SearchInsensitive()`, `Rank()` ✅
- [x] Operators for use in Where: `SearchOp()`, `SearchOpWithConfig()` ✅
- [x] `ExecuteFullTextSearch()` function with automatic rank ✅
- [x] `BuildFullTextIndex()` helper to create GIN indexes ✅
- [ ] MySQL support (deferred to Phase 8)

---

## ✅ Phase 8: Multi-Database Support - **COMPLETED**

### 8.1 Dialect Abstraction ✅

- [x] Create `Dialect` interface to abstract database differences ✅
- [x] Implement PostgreSQL dialect ✅
- [x] Implement MySQL dialect ✅
- [x] Implement SQLite dialect ✅
- [x] Adapt migrations to use dialects ✅
- [x] Adapt query builder to use dialects ✅
- [x] Replace hardcoded placeholders ($1, $2) with dialect.GetPlaceholder() ✅
- [x] Adapt JSON operations to use dialect methods ✅
- [x] Adapt full-text search to use dialect.GetFullTextSearchQuery() ✅
- [x] Adapt LIMIT/OFFSET to use dialect.GetLimitOffsetSyntax() ✅
- [x] Use dialect.QuoteIdentifier() for all identifiers ✅
- [x] **Drivers as optional dependencies** ✅
  - [x] Build tag system for drivers (pgx, mysql, sqlite) ✅
  - [x] Drivers moved to indirect dependencies ✅
  - [x] Stubs for tests when drivers are not available ✅
  - [x] Separate tests per driver with build tags ✅
  - [x] No driver is a direct dependency of the project ✅

### 8.2 MySQL Support ✅

- [x] Implement MySQL dialect ✅
- [x] Adapt data types ✅
- [x] MySQL migration support ✅
- [x] MySQL integration tests (with build tags) ✅
- [x] MySQL driver as optional dependency (indirect) ✅

### 8.3 SQLite Support ✅

- [x] Implement SQLite dialect ✅
- [x] Adapt data types ✅
- [x] SQLite migration support ✅
- [x] Useful for testing and development ✅
- [x] SQLite integration tests (with build tags) ✅
- [x] SQLite driver as optional dependency (indirect) ✅

### 8.4 SQL Server Support (future) ⏳

- [ ] Implement SQL Server dialect
- [ ] Adapt specific syntax

---

## ✅ Phase 9: Tools and Utilities - **COMPLETED**

### 9.1 `prisma studio` Command (optional) ⏳

- [ ] Web interface to view and edit data
- [ ] Alternative: integration with existing tools
- **Note**: Deferred to future version (requires web server)

### 9.2 Logging and Debug ✅

- [x] Logging system with levels (query, info, warn, error) ✅
- [x] SQL query logs with duration ✅
- [x] Debug mode: `prisma.conf` with `log = ["query", "info", "warn", "error"]` ✅
- [x] Query formatting with arguments ✅
- [x] File logger ✅
- [ ] Performance metrics (deferred to future version)

### 9.3 Connection Pooling ✅

- [x] Connection pool configuration ✅
- [x] Configuration via `prisma.conf` (datasource.pool) ✅
- [x] Functions for pool statistics ✅
- [x] Health checks for connections ✅
- [x] `CheckHealth()` and `CheckHealthWithPool()` functions ✅
- [ ] pgbouncer and proxy support (deferred to future version)

### 9.4 Prisma Migrate Diff ✅

- [x] `prisma migrate diff` command implemented ✅
- [x] Compare two schemas (.prisma file) ✅
- [x] Compare schema with database (`--from schema.prisma --to database`) ✅
- [x] Compare database with schema (`--from database --to schema.prisma`) ✅
- [x] Generate migration SQL between two states ✅
- [x] `--out` option to save migration to file ✅
- [x] Support for Prisma functions (autoincrement, now, etc.) ✅
- [x] Useful for synchronization between environments ✅

---

## ✅ Phase 10: Documentation and Examples - **COMPLETED**

### 10.1 Complete Documentation ✅

- [x] Basic README ✅
- [x] Quick start guide (docs/QUICKSTART.md) ✅
- [x] Complete API reference (docs/API.md) ✅
- [x] Migrations guide (docs/MIGRATIONS.md) ✅
- [x] Relationships guide (docs/RELATIONSHIPS.md) ✅
- [x] Best practices (docs/BEST_PRACTICES.md) ✅
- [x] Examples overview (docs/EXAMPLES.md) ✅

### 10.2 Practical Examples ✅

- [x] Simple Query Builder and Raw SQL examples in docs/EXAMPLES.md ✅
- [x] Complete beginner-friendly Quick Start guide in docs/QUICKSTART.md ✅
- [x] Examples for PostgreSQL, MySQL and SQLite ✅

### 10.3 Tests ✅

- [x] Unit tests for CLI package (`cli/command_test.go`) ✅
- [x] Unit tests for all CLI commands ✅
  - [x] `init` (8 tests) ✅
  - [x] `generate` (9 tests) ✅
  - [x] `validate` (6 tests) ✅
  - [x] `format` (5 tests) ✅
  - [x] `migrate dev` (4 tests) ✅
  - [x] `migrate deploy` (3 tests) ✅
  - [x] `migrate reset` (3 tests) ✅
  - [x] `migrate status` (3 tests) ✅
  - [x] `migrate resolve` (4 tests) ✅
  - [x] `migrate diff` (5 tests) ✅
  - [x] `db push` (4 tests) ✅
  - [x] `db pull` (3 tests) ✅
  - [x] `db seed` (4 tests) ✅
  - [x] `db execute` (5 tests) ✅
- [x] End-to-end integration tests (`integration_test.go`) ✅
- [x] Reusable test helpers (`test_helpers.go`) ✅
- [x] **Total: 93 tests implemented** ✅
- [x] **Tests with different databases** ✅
  - [x] PostgreSQL tests with `pgx` build tag ✅
  - [x] MySQL tests with `mysql` build tag ✅
  - [x] SQLite tests with `sqlite` build tag ✅
  - [x] Test database setup/teardown system ✅
  - [x] Query builder tests for each driver ✅
  - [x] Migration tests for each driver ✅
  - [x] Driver adapter tests for each driver ✅
  - [x] Test documentation (docs/TESTING.md) ✅

---

## ✅ Phase 11: Publication and Community - **COMPLETED**

### 11.1 Release Preparation ✅

- [x] Semantic versioning ✅
- [x] Detailed changelog ✅
- [x] CI/CD with GitHub Actions ✅
- [x] Automated releases ✅

### 11.2 Distribution ✅

- [x] README updated in English ✅
- [x] CLI binaries for multiple platforms (Linux, macOS, Windows) ✅
- [x] Installation via `go install` ✅
- [ ] Publish to pkg.go.dev (requires module publication)
- [ ] Homebrew formula (macOS) - deferred until module is published
- [ ] APT/YUM packages (Linux) - deferred until module is published

### 11.3 Community ✅

- [x] Issue and PR templates ✅
- [x] Contribution guide (CONTRIBUTING.md) ✅
- [x] Code of conduct (CODE_OF_CONDUCT.md) ✅
- [x] Pull request template ✅
- [ ] Discord/Slack for community (deferred to future version)
- [ ] Blog posts and tutorials (deferred to future version)

---

## 📋 Features Already Implemented (Before Roadmap)

### Query Builder ✅

- [x] Basic Query Builder with Prisma-style API
- [x] WHERE operators (Gt, Gte, Lt, Lte, In, NotIn, Like, ILike, IsNull, IsNotNull)
- [x] Methods: FindFirst, FindMany, Count, Create, Update, Delete
- [x] Soft delete support (via `deleted_at`)
- [x] Automatic timestamps (`created_at`, `updated_at`)

### Raw SQL ✅

- [x] Raw SQL executor
- [x] Methods: Query, QueryRow, Exec

### Code Generation ✅

- [x] Basic code generation structure
- [x] Model generation
- [x] Wrapper generation
- [x] Type generation
- [x] SQL query generation

---

## 🎯 Implementation Priorities

### High Priority (MVP)

1. ✅ Configuration system (prisma.conf) - **COMPLETED**
2. ✅ Basic schema.prisma parser (models, fields, primitive types) - **COMPLETED**
3. ✅ CLI: `generate`, `validate` - **COMPLETED**
4. ✅ Type-safe code generation - **COMPLETED**
5. ✅ Basic migrations: `migrate dev`, `migrate deploy`, `migrate reset` - **COMPLETED**
6. ✅ `db push` and `db pull` - **COMPLETED**
7. ✅ Native CLI (no external dependencies) - **COMPLETED**
8. ✅ Complete test suite (93+ tests) - **COMPLETED**

### Medium Priority

7. ✅ Transactions - **COMPLETED**
8. ✅ Advanced query operators - **COMPLETED**
9. ⏳ Relationships and eager loading (basic structure implemented)
10. ⏳ Enum and index support (partial)
11. ⏳ Middleware and hooks

### Low Priority (Future)

12. ✅ Complex aggregations - **COMPLETED** (Count, Sum, Avg, Min, Max, GroupBy)
13. ✅ Full-text search - **COMPLETED** (PostgreSQL)
14. ✅ MySQL and SQLite support - **COMPLETED**
15. ⏳ Prisma Studio
16. ✅ Advanced validations - **COMPLETED** (runtime validation)

---

## 📝 Notes

- ✅ = Completed
- ⏳ = In progress or pending
- 🚧 = Actively in development

**Last updated**: 2025-12-06

### 🎉 Recent Improvements

- **100% Type-Safe Fluent API**: Complete implementation of fluent API with total type safety
  - `Create().Data(CreateInput).Exec(ctx)` - Type-safe create operations
  - `FindMany().Select(Select).Where(WhereInput).Exec(ctx)` - Type-safe queries
  - `FindFirst().Select(Select).Where(WhereInput).Exec(ctx)` - Type-safe single record queries
  - `Update().Where(WhereInput).Data(UpdateInput).Exec(ctx)` - Type-safe updates
  - `Delete().Where(WhereInput).Exec(ctx)` - Type-safe deletes
  - Select types (UserSelect, PostSelect, etc.) for field selection
  - WhereInput with Filter types (StringFilter, IntFilter, DateTimeFilter, etc.)
  - Logical operators: AND, OR, NOT in WhereInput
- **Optional Drivers with Build Tags**: Complete system of optional drivers
  - Drivers as indirect dependencies (not compiled in main binary)
  - Build tags for tests (pgx, mysql, sqlite)
  - Automatic stubs when drivers are not available
  - Separate tests per driver with build tags
- **Driver Abstraction**: Completely driver-agnostic system, removing direct pgx dependency
- **Beginner-Friendly Documentation**: Complete Quick Start guide and simple examples for beginners
- **Multi-Driver Support**: Users can choose their own driver (pgx, mysql, sqlite3)
- **Native CLI**: Complete migration from Cobra to native Go implementation, removing external dependencies
- **Complete Test Suite**: 93+ tests implemented covering all CLI commands, main features and multiple drivers
- **Multi-Driver Tests**: Integration tests for PostgreSQL, MySQL and SQLite with build tags
- **Code Generation**: Dynamic imports - only necessary imports are included in generated files
- **Naming**: Models and columns always generated in PascalCase, following Go conventions
- **SQL Injection Prevention**: All identifiers escaped and values using prepared parameters
- **UUID Support**: Complete support for `@id @default(dbgenerated("gen_random_uuid()")) @db.Uuid` with automatic extension creation
- **Code Cleanup**: Removal of unused code, translation of comments to English, warning fixes
