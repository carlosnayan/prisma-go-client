// {{.PascalName}}Query is the query builder for model {{.ModelName}} with fluent API
// All methods from builder.Query are available via embedding
// Example: q.Where("email = ?", "user@example.com").First(ctx, &user)
type {{.PascalName}}Query struct {
	*builder.Query
}


