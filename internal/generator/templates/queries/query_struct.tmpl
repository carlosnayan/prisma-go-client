// {{.PascalName}}Query is the query builder for model {{.ModelName}} with fluent API
// Access to builder.Query methods via the embedded BaseQuery field
// Example: q.BaseQuery.Where("email = ?", "user@example.com").First(ctx, &user)
type {{.PascalName}}Query struct {
	Query *builder.Query
}
